{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": "The AI key is deliberately empty here. Supply it as Ai__ApiKey in the environment (see docker-compose.yml) or with dotnet user-secrets in development. A key committed to a repository is a key that has to be rotated.",
  "Ai": {
    "ApiKey": "",
    "Provider": "Gemini",
    "Model": "",
    "BaseUrl": "",
    "TimeoutSeconds": 300
  }
}
